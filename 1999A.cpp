#include <bits/stdc++.h>

using ll=long long;
using namespace std;

int32_t main() {
    int t;
    cin >> t;

    while (t--) {
        string n;
        cin >> n;

        cout << (n[0] - '0') + (n[1]-'0') << '\n';
    }

    return 0;
}
